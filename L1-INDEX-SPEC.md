# L1 索引文件规范

被 SKILL.md 的「L1 元数据索引层」与「初始化」步骤引用。索引文件是 vault 的常驻元数据层：每一行是一个知识条目，只有「名称 + 一句话简介 + 链接」，正文细节一律留在 L2。

## 条目格式

两种等价形式，目录索引用表格，导航区/组件地图用 bullet：

```markdown
| 名称 | 一句话简介 | 链接 |
|---|---|---|
| Auth Middleware | 认证中间件：会话校验与令牌刷新 | [[Auth Middleware]] |
```

```markdown
- [[Auth Middleware]] — 认证中间件：会话校验与令牌刷新
```

一句话简介的写法：主语 + 做什么，15 字左右。写的是「这个知识能解决什么」，不是「这个知识是什么」——索引行的工作是让意图判定能命中，不是做介绍。

## 索引文件位置

| 索引文件 | 覆盖范围 |
|---|---|
| `Home.md` 导航区 | vault 全局：项目、领域、模式、会话、待办 |
| `projects/Projects.md` | 所有项目（一行一个项目） |
| `projects/{项目}/{项目}.md` 的「组件地图」区 | 该项目的组件/ADR/模式 |
| `domains/Domains.md` | 跨项目领域知识（一行一个领域） |
| `patterns/Universal Patterns.md` | 通用模式（一行一个模式） |
| `deep/README.md` 的「内容清单」 | 深水长文档（一行一个文档） |
| `examples/` 的登记说明 | few-shot 示例库（一行一个示例主题） |

## 维护规则

1. **先索引，后正文**：写任何新笔记前，先在对应索引加一行；写完笔记后核对链接可解析。
2. **一行永不超过一句**：索引行膨胀到两句时，把细节移进笔记正文。
3. **删除笔记时删除索引行**：孤儿索引行是意图判定的噪音。
4. **更新笔记时核对简介**：笔记职责变了，简介要跟着变，否则判定会命中错目标。

## 完成标准

每个索引文件自检：任取一行，能凭「一句话简介」判定它是否匹配一个给定意图，且无需读正文。全部条目链接指向存在的笔记文件。
