# L3 深水检索协议

被 SKILL.md 的「L3 深水执行层」引用。L3 只在三个触发条件下开启，其余时间完全关闭。它的输出是**片段级注入**，绝不把整篇长文档拉进上下文。

## 触发条件（满足其一）

1. **工具调用返回错误**——先查错误是否由未知业务规则导致，再检索对应规则
2. **执行超时**——任务触达长流程，检索流程文档定位卡点
3. **极长业务逻辑**——用户需求指向复杂规则文档、长流程说明（数千字以上的规则/流程资料）

## 检索路径（按序执行，命中即停）

### 1. grep 全文定位

```powershell
grep pattern="关键词" path="deep/"
```

用关键词定位候选文件与行号（grep 工具语义：`pattern` 是正则，`path` 是检索目录）。**完成标准**：得到一个文件名 + 行号区间，且区间覆盖了需要的上下文。

### 2. 定向片段读取

```powershell
read <文件路径> offset=<起始行> limit=<行数>
```

只读区间内的片段（前后各留 5 行余量）。**完成标准**：片段足以回答当前问题；若不够，回第 1 步换关键词。

### 3. wikilink 图遍历（可选）

若片段指向其他文档（`[[链接]]`），沿链接走到相关节点，重复第 1-2 步。**完成标准**：遍历深度 ≤ 2，每次只带一个目标链接。

### 4. 向量库检索（若配置）

配置检测：vault 中存在 embedding 索引文件（如 `.vectors/`、`index.json` 等向量索引物）即为已配置。已配置则对片段做语义检索，未配置则跳过——grep + 图遍历已覆盖 L3 的全部触发场景。**完成标准**：命中片段与问题相关，或确认无相关片段后终止检索。

## 注入规范

- 每次注入只带**当前问题所需的片段**，带来源标注（文件 + 行号）
- 问题解决后，片段从上下文退出：不滞留、不复述、不写回原文
- 检索到的规则若被证明是长期有效的，提炼成一句 L2 笔记 + L1 索引行，长文档仍留在 deep/

## 常态边界

- 正常会话中 deep/ 目录不参与任何读取
- 访问 deep/ 前先确认命中三触发之一
- 检索以「找到答案」为终点，获取片段即止，读完即走
