---
name: sleep-thinking
description: 管理知识进入上下文的时机与粒度，以最少 token 供给精准知识。Use when 会话开始定位记忆、意图匹配某技能或笔记需要决定注入多少内容、工具调用报错或超时、处理极长业务规则与流程文档，或用户要求优化上下文、搭建知识加载流程、初始化记忆索引。
---

# 三层知识加载协议

知识只通过三层进入上下文：L1 常驻「名称 + 一句话」，L2 命中注入全文，L3 触发时开闸检索片段。三层之外的知识视为不存在，直到被某一层拉进上下文。

## L1 元数据索引层（常驻）

**内容**：每个知识的「名称 + 一句话简介」，仅此而已。无 Schema、无参数、无示例。

**载体**：

- 技能系统：每个 skill 的 `name` + `description`（已常驻系统提示词，即本层）
- vault 索引文件：`Home.md` 导航区、`Projects.md`、`Domains.md`、`patterns/Universal Patterns.md` 的条目表，以及 `projects/{项目}/{项目}.md` 的「组件地图」区

**会话开始动作**：执行记忆协议中的会话定位——读 `Active TODOs.md` + 当前项目概览，构建全局知识视图（若使用 DSH.md，即其对话 2）。

**维护规则**：新增任何知识，先在对应索引加一行「名称 + 一句话简介 + 链接」，再写笔记正文。索引行格式见 L1-INDEX-SPEC.md。

## L2 意图理解层（动态注入）

**触发**：用户输入被判定为匹配某一具体技能或笔记时。

**时序铁律**：先判定意图 → 再注入 → 后生成调用动作。预取是浪费，误判注入是污染。

**注入内容**（三者缺一不可）：

1. 完整 Schema：笔记 frontmatter 全字段，或技能的工具/参数结构
2. 全部参数定义：每个字段的含义、取值、默认值
3. 2-3 个高质量 few-shot 示例：覆盖主路径与一个边界分支

**载体**：

- 技能：调用 `skill` 工具加载 SKILL.md 全文
- 笔记：`read` 笔记文件全文（frontmatter = Schema，正文 = 定义，「示例」区 = few-shot）

**few-shot 存放**：vault 笔记的「示例」区，或 `examples/` 目录。每条示例 = 输入 + 预期输出 + 一句为什么。

## L3 深水执行层（按需 RAG）

**触发**（满足其一才进入）：

1. 工具调用返回错误
2. 执行超时
3. 需处理极长外部业务逻辑（复杂规则文档、长流程说明）

**检索路径**：`grep` 全文 → `[[wikilink]]` 图遍历 → 向量库（若配置）。完整协议见 L3-DEEP-RETRIEVAL.md。

**常态**：不参与任何上下文构建。庞大资料库彻底脱离系统提示词与常规对话。

## vault 目录对照

| vault 位置 | 所属层 |
|---|---|
| 索引文件（Home 导航、Projects、Domains、组件地图） | L1 |
| 笔记全文（components/ADR/patterns/domains 笔记） | L2 |
| `deep/` 长文档 | L3 |
| `examples/` few-shot 库 | L2 注入引用 |

## 会话生命周期步骤

### 1. 启动定位（L1）

读 `todos/Active TODOs.md` + 当前项目概览。**完成标准**：能说出当前项目、欠着的工作、可用的知识域清单（每个一句话），且只读了这两个文件。

### 2. 请求处理（L2 判定）

判定用户意图匹配哪个知识。匹配则注入其完整内容（Schema + 参数 + few-shot），然后生成动作。**完成标准**：注入发生在动作生成之前，且只注入了匹配项，未触碰其他笔记。

### 3. 深水触发（L3）

命中错误/超时/长业务逻辑时，按 L3-DEEP-RETRIEVAL.md 检索。**完成标准**：检索范围以片段为单位注入，检索结束后报告注入来源，不滞留全文。

### 4. 收尾回写

执行记忆协议中的收尾回写（若使用 DSH.md，即其对话 5 的 recap），并把本次新增知识补进 L1 索引。**完成标准**：session 总结已写、索引行已加、TODOs 已更新。

## 初始化（首次使用）

1. 确认 vault 路径（`OBSIDIAN_VAULT_PATH`，缺则询问）
2. 按 L1-INDEX-SPEC.md 为 vault 建立索引文件
3. 创建 `deep/` 与 `examples/` 目录
4. **完成标准**：vault 中每个知识域都有索引条目，deep/ 与常规笔记分离，examples/ 有登记说明

## 红线

- 索引行永远一句话，正文细节留在 L2
- 意图判定先于注入，动作生成后于注入
- L3 只响应错误、超时、长业务逻辑三个触发
- 索引与笔记成对维护：先索引行，后笔记正文
- 每层只消费本层内容
